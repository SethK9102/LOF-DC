Images folder — placeholder art

This folder contains simple SVG placeholder images for the LOF-DC project. Replace these with final artwork (PNG/SVG) later. Filenames and intended use:

- `player.svg` — player avatar / icon
- `enemy_basic.svg` — generic enemy
- `enemy_boss.svg` — boss enemy
- `companion.svg` — companion / summon
- `item_health_potion.svg` — health potion icon
- `item_mana_potion.svg` — mana potion icon
- `sword.svg` — weapon icon
- `shield.svg` — defensive equipment icon
- `armor.svg` — armor equipment icon
- `shop.svg` — store / vendor icon
- `floor_tile.svg` — floor / map tile placeholder
- `boss_room.svg` — boss room / arena background
- `ui_button.svg` — generic UI button/icon
- `logo.svg` — project logo placeholder

Notes:
- These are simple SVGs created as placeholders so the UI and scenes can be prototyped immediately.
- To replace: add higher-quality PNG or SVG files with the same filenames, or update code to reference new asset paths.
